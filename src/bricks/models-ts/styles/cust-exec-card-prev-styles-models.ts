export interface ICustExecContainer {
  width: string,
  height: string,
  backgroundColor: string,
  mb: string | null,
  mar: string | null,
}

// eslint-disable-next-line import/no-anonymous-default-export
export default () => false && console.log('интерфейсы проэкспортированы')